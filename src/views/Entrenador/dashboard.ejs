<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Entrenador</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --black: #121212;
            --dark: #1E1E1E;
            --gray: #2D2D2D;
            --light: #F5F5F5;
            --accent: #E74C3C;
            --accent-hover: #C0392B;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--black);
            color: var(--light);
            min-height: 100vh;
        }

        .dashboard-container {
            display: grid;
            grid-template-columns: 280px 1fr;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            background: var(--dark);
            padding: 30px 0;
            border-right: 1px solid var(--gray);
        }

        .user-profile {
            display: flex;
            align-items: center;
            padding: 0 25px 25px;
            border-bottom: 1px solid var(--gray);
            margin-bottom: 25px;
        }

        .user-profile .avatar {
            width: 50px;
            height: 50px;
            background-color: var(--accent);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 15px;
            color: white;
            font-weight: bold;
            font-size: 1.2rem;
        }

        .user-profile .info h2 {
            font-size: 1.1rem;
            margin-bottom: 5px;
        }

        .user-profile .info p {
            color: #AAA;
            font-size: 0.9rem;
        }

        .nav-menu {
            padding: 0 15px;
        }

        .nav-menu h3 {
            color: #777;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 15px;
            padding-left: 10px;
        }

        .nav-menu ul {
            list-style: none;
            margin-bottom: 30px;
        }

        .nav-menu li a {
            display: flex;
            align-items: center;
            padding: 12px 15px;
            color: var(--light);
            text-decoration: none;
            border-radius: 6px;
            transition: all 0.3s;
            margin-bottom: 5px;
        }

        .nav-menu li a:hover,
        .nav-menu li a.active {
            background-color: var(--gray);
            color: white;
        }

        .nav-menu li a i {
            width: 25px;
            font-size: 1rem;
            margin-right: 10px;
            color: var(--accent);
        }

        /* Main Content */
        .main-content {
            padding: 30px;
            background-color: var(--black);
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 1.8rem;
            color: var(--light);
        }

        .stats-cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: var(--dark);
            border-radius: 8px;
            padding: 20px;
            border-left: 4px solid var(--accent);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        .stat-card h3 {
            color: #AAA;
            font-size: 0.9rem;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }

        .stat-card h3 i {
            margin-right: 8px;
            color: var(--accent);
        }

        .stat-card .value {
            font-size: 1.8rem;
            font-weight: bold;
            color: white;
        }

        .dashboard-sections {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .section-card {
            background: var(--dark);
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            border: 1px solid var(--gray);
        }

        .section-card h2 {
            background: var(--gray);
            color: white;
            padding: 15px 20px;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
        }

        .section-card h2 i {
            margin-right: 10px;
            color: var(--accent);
        }

        .section-card ul {
            padding: 20px;
            list-style: none;
        }

        .section-card li {
            margin-bottom: 12px;
        }

        .section-card a {
            display: flex;
            align-items: center;
            padding: 10px;
            color: var(--light);
            text-decoration: none;
            border-radius: 4px;
            transition: all 0.3s;
        }

        .section-card a:hover {
            background: var(--gray);
            padding-left: 15px;
        }

        .section-card a i {
            width: 25px;
            font-size: 0.9rem;
            color: var(--accent);
        }

        @media (max-width: 768px) {
            .dashboard-container {
                grid-template-columns: 1fr;
            }

            .sidebar {
                display: none;
            }
        }
    </style>
</head>

<body>
    <div class="dashboard-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="user-profile">
                <div class="avatar">
                    <%= usuario.nombre.charAt(0).toUpperCase() %>
                </div>
                <div class="info">
                    <h2>
                        <%= usuario.nombre %>
                    </h2>
                    <p>Entrenador</p>
                </div>
            </div>

            <nav class="nav-menu">
                <h3>Navegación</h3>
                <ul>
                    <li><a href="/logout"><i class="fas fa-sign-out-alt"></i> Cerrar Sesión</a></li>
                </ul>

                <h3>Gestión General</h3>
                <ul>
                    <li><a href="/entrenador/clients"><i class="fas fa-users"></i> Clientes</a></li>
                    <li><a href="/entrenador/rutinas"><i class="fas fa-dumbbell"></i> Rutinas</a></li>
                    <li><a href="/entrenador/dietas"><i class="fas fa-utensils"></i> Dietas</a></li>
                </ul>

                <h3>Comunicación</h3>
                <ul>
                    <li><a href="/entrenador/mensajes"><i class="fas fa-envelope"></i> Mensajes</a></li>
                </ul>

                <h3>Perfil</h3>
                <ul>
                    <li><a href="/entrenador/perfil"><i class="fas fa-user-cog"></i> Mi Perfil</a></li>
                </ul>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <div class="header">
                <h1>Panel de Entrenador</h1>
            </div>

            <div class="stats-cards">
                <div class="stat-card">
                    <h3><i class="fas fa-users"></i> Clientes Activos</h3>
                    <div class="value">24</div>
                </div>
                <div class="stat-card">
                    <h3><i class="fas fa-dumbbell"></i> Rutinas Creadas</h3>
                    <div class="value">56</div>
                </div>
                <div class="stat-card">
                    <h3><i class="fas fa-utensils"></i> Planes Dietéticos</h3>
                    <div class="value">32</div>
                </div>
                <div class="stat-card">
                    <h3><i class="fas fa-envelope"></i> Mensajes Nuevos</h3>
                    <div class="value">5</div>
                </div>
            </div>

            <div class="dashboard-sections">
                <!-- Gestión General -->
                <section class="section-card">
                    <h2><i class="fas fa-cogs"></i> Gestión General</h2>
                    <ul>
                        <li><a href="/entrenador/clients"><i class="fas fa-chevron-right"></i> Gestionar Clientes</a>
                        </li>
                        <li><a href="/entrenador/rutinas"><i class="fas fa-chevron-right"></i> Gestionar Rutinas</a>
                        </li>
                        <li><a href="/entrenador/dietas"><i class="fas fa-chevron-right"></i> Gestionar Dietas</a></li>
                    </ul>
                </section>

                <!-- Comunicación -->
                <section class="section-card">
                    <h2><i class="fas fa-comments"></i> Comunicación</h2>
                    <ul>
                        <li><a href="/entrenador/mensajes"><i class="fas fa-chevron-right"></i> Mensajes</a></li>
                    </ul>
                </section>

                <!-- Perfil -->
                <section class="section-card">
                    <h2><i class="fas fa-user"></i> Perfil</h2>
                    <ul>
                        <li><a href="/entrenador/perfil"><i class="fas fa-chevron-right"></i> Editar Perfil</a></li>
                    </ul>
                </section>
            </div>
        </main>
    </div>
</body>

</html>