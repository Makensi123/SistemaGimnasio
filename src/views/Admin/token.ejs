<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        :root {
            --black: #121212;
            --dark: #1E1E1E;
            --gray: #2D2D2D;
            --light: #F5F5F5;
            --accent: #E74C3C;
            --primary: #3498DB;
            --primary-hover: #2980B9;
            --success: #2ECC71;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--black) 0%, var(--dark) 100%);
            color: var(--light);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 30px;
            background: linear-gradient(45deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-align: center;
        }

        h2 {
            font-size: 1.8rem;
            font-weight: 600;
            margin: 40px 0 20px 0;
            color: var(--light);
            opacity: 0.9;
        }

        .back-link {
            display: inline-block;
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
            padding: 10px 20px;
            border: 2px solid var(--primary);
            border-radius: 8px;
            margin-bottom: 30px;
            transition: all 0.3s ease;
        }

        .back-link:hover {
            background-color: var(--primary);
            color: var(--light);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
        }

        .form-container {
            background: var(--gray);
            padding: 30px;
            border-radius: 12px;
            margin-bottom: 40px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        form button {
            background: linear-gradient(135deg, var(--success), #27AE60);
            color: var(--light);
            border: none;
            border-radius: 8px;
            padding: 15px 30px;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        form button:hover {
            background: linear-gradient(135deg, #27AE60, var(--success));
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(46, 204, 113, 0.4);
        }

        form button:active {
            transform: translateY(0);
        }

        .table-container {
            background: var(--gray);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: transparent;
        }

        thead {
            background: linear-gradient(135deg, var(--primary), var(--primary-hover));
        }

        th {
            padding: 15px 12px;
            text-align: left;
            font-weight: 600;
            color: var(--light);
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        tbody tr {
            border-bottom: 1px solid rgba(245, 245, 245, 0.1);
            transition: all 0.3s ease;
        }

        tbody tr:hover {
            background-color: rgba(52, 152, 219, 0.1);
            transform: scale(1.005);
        }

        tbody tr:last-child {
            border-bottom: none;
        }

        td {
            padding: 15px 12px;
            color: var(--light);
            font-size: 0.95rem;
        }

        .token-cell {
            font-family: 'Courier New', monospace;
            font-size: 0.85rem;
            background: var(--dark);
            border-radius: 4px;
            padding: 8px;
            word-break: break-all;
            border: 1px solid rgba(245, 245, 245, 0.1);
        }

        .status-available {
            color: var(--success);
            font-weight: 600;
            padding: 4px 8px;
            background: rgba(46, 204, 113, 0.2);
            border-radius: 4px;
        }

        .status-used {
            color: var(--accent);
            font-weight: 600;
            padding: 4px 8px;
            background: rgba(231, 76, 60, 0.2);
            border-radius: 4px;
        }

        .date-cell {
            color: var(--light);
            opacity: 0.8;
        }

        @media (max-width: 768px) {
            body {
                padding: 10px;
            }

            h1 {
                font-size: 2rem;
            }

            h2 {
                font-size: 1.5rem;
            }

            .form-container {
                padding: 20px;
            }

            form button {
                padding: 12px 24px;
                font-size: 1rem;
            }

            .table-container {
                overflow-x: auto;
            }

            table {
                min-width: 600px;
            }

            th,
            td {
                padding: 10px 8px;
                font-size: 0.85rem;
            }

            .token-cell {
                font-size: 0.75rem;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 0 5px;
            }

            h1 {
                font-size: 1.8rem;
            }

            .back-link {
                padding: 8px 16px;
                font-size: 0.9rem;
            }

            .form-container {
                padding: 15px;
            }

            form button {
                padding: 10px 20px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Generar Token</h1>
        <a href="/admin/dashboard" class="back-link">Volver</a>

        <div class="form-container">
            <form action="/admin/tokens/generar" method="POST">
                <button type="submit">Generar nuevo token</button>
            </form>
        </div>

        <h2>Tokens generados</h2>
        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Token</th>
                        <th>Estado</th>
                        <th>Creado en</th>
                    </tr>
                </thead>
                <tbody>
                    <% tokens.forEach(token=> { %>
                        <tr>
                            <td>
                                <div class="token-cell">
                                    <%= token.token %>
                                </div>
                            </td>
                            <td>
                                <span class="<%= token.usado ? 'status-used' : 'status-available' %>">
                                    <%= token.usado ? 'Usado' : 'Disponible' %>
                                </span>
                            </td>
                            <td class="date-cell">
                                <%= token.creadoEn.toLocaleString() %>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>

</html>