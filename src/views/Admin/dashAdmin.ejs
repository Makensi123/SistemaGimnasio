<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel de Administración</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --black: #121212;
            --dark: #1E1E1E;
            --gray: #2D2D2D;
            --light: #F5F5F5;
            --accent: #E74C3C;
            --accent-hover: #C0392B;
            --primary: #3498DB;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--black);
            color: var(--light);
            min-height: 100vh;
        }

        .admin-container {
            display: grid;
            grid-template-columns: 250px 1fr;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            background: var(--dark);
            color: white;
            padding: 30px 0;
            border-right: 1px solid var(--gray);
        }

        .admin-profile {
            text-align: center;
            padding: 20px;
            border-bottom: 1px solid var(--gray);
            margin-bottom: 30px;
        }

        .admin-profile .avatar {
            width: 70px;
            height: 70px;
            background-color: var(--accent);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            color: white;
            font-weight: bold;
            font-size: 1.5rem;
        }

        .admin-profile h2 {
            font-size: 1.2rem;
            margin-bottom: 5px;
        }

        .admin-profile p {
            color: #AAA;
            font-size: 0.9rem;
        }

        .nav-menu {
            padding: 0 20px;
        }

        .nav-menu h3 {
            color: #777;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 15px;
            padding-left: 10px;
        }

        .nav-menu ul {
            list-style: none;
            margin-bottom: 30px;
        }

        .nav-menu li a {
            display: flex;
            align-items: center;
            padding: 12px 15px;
            color: var(--light);
            text-decoration: none;
            border-radius: 6px;
            transition: all 0.3s;
            margin-bottom: 5px;
        }

        .nav-menu li a:hover {
            background-color: var(--gray);
            color: white;
        }

        .nav-menu li a i {
            width: 25px;
            font-size: 1rem;
            margin-right: 10px;
            color: var(--primary);
        }

        .nav-menu li a.logout {
            color: var(--accent);
        }

        .nav-menu li a.logout:hover {
            background-color: rgba(231, 76, 60, 0.1);
        }

        /* Main Content */
        .main-content {
            padding: 30px;
            background-color: var(--black);
        }

        .admin-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .admin-header h1 {
            font-size: 1.8rem;
            color: var(--light);
        }

        .stats-cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: var(--dark);
            border-radius: 8px;
            padding: 20px;
            border-left: 4px solid var(--primary);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        .stat-card h3 {
            color: #AAA;
            font-size: 0.9rem;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }

        .stat-card h3 i {
            margin-right: 8px;
            color: var(--primary);
        }

        .stat-card .value {
            font-size: 1.8rem;
            font-weight: bold;
            color: white;
        }

        .dashboard-sections {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
        }

        .section-card {
            background: var(--dark);
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            border: 1px solid var(--gray);
        }

        .section-card h2 {
            background: var(--gray);
            color: white;
            padding: 15px 20px;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
        }

        .section-card h2 i {
            margin-right: 10px;
            color: var(--primary);
        }

        .section-card ul {
            padding: 20px;
            list-style: none;
        }

        .section-card li {
            margin-bottom: 12px;
        }

        .section-card a {
            display: flex;
            align-items: center;
            padding: 10px;
            color: var(--light);
            text-decoration: none;
            border-radius: 4px;
            transition: all 0.3s;
        }

        .section-card a:hover {
            background: var(--gray);
            padding-left: 15px;
        }

        .section-card a i {
            width: 25px;
            font-size: 0.9rem;
            color: var(--primary);
        }

        @media (max-width: 768px) {
            .admin-container {
                grid-template-columns: 1fr;
            }

            .sidebar {
                display: none;
            }
        }
    </style>
</head>

<body>
    <div class="admin-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="admin-profile">
                <div class="avatar">
                    <%= usuario.nombre.charAt(0).toUpperCase() %>
                </div>
                <div class="info">
                    <h2>
                        <%= usuario.nombre %>
                    </h2>
                    <p>Administrador</p>
                </div>
            </div>

            <nav class="nav-menu">
                <ul>
                    <li><a href="/logout" class="logout"><i class="fas fa-sign-out-alt"></i> Cerrar Sesión</a></li>
                </ul>

                <h3>Panel de Administración</h3>
                <ul>
                    <li><a href="/admin/clients"><i class="fas fa-users"></i> Gestionar Clientes</a></li>
                    <li><a href="/admin/entrenadores"><i class="fas fa-user-tie"></i> Gestionar Entrenadores</a></li>
                    <li><a href="/admin/tokens"><i class="fas fa-key"></i> Generar Tokens</a></li>
                    <li><a href="/admin/ejercicios"><i class="fas fa-dumbbell"></i> Ejercicios</a></li>
                </ul>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <div class="admin-header">
                <h1>Panel de Administración</h1>
            </div>

            <div class="stats-cards">
                <div class="stat-card">
                    <h3><i class="fas fa-users"></i> Clientes Activos</h3>
                    <div class="value">
                        <%= cantidadClientes %>
                    </div>
                </div>
                <div class="stat-card">
                    <h3><i class="fas fa-user-clock"></i> Sin Entrenador</h3>
                    <div class="value">
                        <%= clientesSinEntrenador %>
                    </div>
                </div>
                <div class="stat-card">
                    <h3><i class="fas fa-user-tie"></i> Entrenadores</h3>
                    <div class="value">
                        <%= cantidadEntrenadores %>
                    </div>
                </div>
                <div class="stat-card">
                    <h3><i class="fas fa-key"></i> Tokens Activos</h3>
                    <div class="value">
                        <%= cantidadTokensActivos %>
                    </div>
                </div>
                <div class="stat-card">
                    <h3><i class="fas fa-dumbbell"></i> Ejercicios</h3>
                    <div class="value">
                        <%= cantidadEjercicios %>
                    </div>
                </div>
            </div>

            <div class="dashboard-sections">
                <section class="section-card">
                    <h2><i class="fas fa-users"></i> Gestión de Clientes</h2>
                    <ul>
                        <li><a href="/admin/clients"><i class="fas fa-chevron-right"></i> Ver todos los clientes</a>
                        </li>

                    </ul>
                </section>
                <!--<section class="section-card">
                    <h2><i class="fas fa-user-tie"></i> Gestión de Entrenadores</h2>
                    <ul>
                        <li><a href="/admin/entrenadores"><i class="fas fa-chevron-right"></i> Ver todos los
                                entrenadores</a></li>

                    </ul>
                </section>-->

                <section class="section-card">
                    <h2><i class="fas fa-key"></i> Tokens de Entrenador</h2>
                    <ul>
                        <li><a href="/admin/tokens"><i class="fas fa-chevron-right"></i> Generar nuevos tokens</a></li>

                    </ul>
                </section>

                <section class="section-card">
                    <h2><i class="fas fa-dumbbell"></i> Gestión de Ejercicios</h2>
                    <ul>
                        <li><a href="/admin/ejercicios"><i class="fas fa-chevron-right"></i> Ver todos los
                                ejercicios</a></li>

                    </ul>
                </section>
            </div>
        </main>
    </div>
</body>

</html>